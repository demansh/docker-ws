# build a container from image.
docker build -t workshop-apache-ubuntu .

# run docker container
docker run --rm -it -p 80:80 --name WORKSHOP_APACHE workshop-apache-ubuntu

# see running containers
docker ps

# go to http://localhost

---

# see stats
docker stats
